import unittest

class TestSavedFoods(unittest.TestCase):

	def test_foodList(self):
		tracker = savedFood()
		tracker.save(“Apple”, 25, 95)
		tracker.save(“Orange, 11,45)
		self.assertEqual(len(tracker.saved), 2)

if __name__ == "__main__":
    unittest.main()
